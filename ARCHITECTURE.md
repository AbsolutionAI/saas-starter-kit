# Architecture

Turborepo monorepo SaaS scaffold (apps/web, packages). Standalone clone; no Aspen runtime.

## Boundaries
- **Standalone:** clone and run per README — no Aspen mesh required
- **Grove:** optional products profile demo
- **Kind:** `node-monorepo`
